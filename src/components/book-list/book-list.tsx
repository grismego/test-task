import React from 'react';
import { Book } from '../book/book';

export const BookList = () => {
    return (
        <div>
            <Book />
            <Book />
            <Book />
        </div>
    );
};
